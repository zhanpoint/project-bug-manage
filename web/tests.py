from django.test import TestCase
# import redis
#
# pool = redis.ConnectionPool(host='localhost', port=6379, password='333444', max_connections=100, )
# conn = redis.Redis(connection_pool=pool)
# conn.set('lzs', '21', ex=60)
# conn.set('lx', '3', ex=60)
